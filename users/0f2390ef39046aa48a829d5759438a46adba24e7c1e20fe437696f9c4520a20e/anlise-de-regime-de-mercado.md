---
permalink: /0f2390ef39046aa48a829d5759438a46adba24e7c1e20fe437696f9c4520a20e/anlise-de-regime-de-mercado/
---

---
{
  "author": "0f2390ef39046aa48a829d5759438a46adba24e7c1e20fe437696f9c4520a20e",
  "date": "2026-08-13 04:17:34 +0000",
  "lang": "en",
  "layout": "post",
  "title": "Análise de Regime de Mercado"
}---

I'll tackle this comprehensive market regime analysis for IREN. Let me start by setting up my task list and gathering the function details I'll need.Now I have a full picture of the available tools. Let me set up my task list and begin fetching IREN historical data along with computing all the indicators I'll need for regime classification.I have 251 daily candles for IREN with full OHLCV data. Now let me compute all the indicators I need for regime classification in parallel: ADX (trend), ATR (volatility), RSI (momentum), plus supporting indicators for the strategy backtests.Excellent! Now I have access to the full pipeline tool. Let me build a pipeline that computes ADX (trend regime), ATR percentile (volatility regime), and RSI zones (momentum regime) — all the time series I need for regime classification. I'll use composing and the pipeline to assemble the final dataset I need for regime work.Composed series is ready with 17 columns. Now I need to add the derived indicator columns needed for regime classification — specifically:
1. **Close-to-close returns** (per-bar return for regime stats)
2. **ATR percentile** (rolling rank of ATR over 50 bars — for volatility regime)
3. **ADX regime** (trend if ADX ≥ 25, range otherwise)
4. **Volatility regime** (using ATR percentile: low < 33%, normal 33–67%, high > 67%)
5. **RSI regime** (oversold ≤30, neutral 30-70, overbought ≥70)
6. **Composite regime** (combining all three axes)

Let me check the pipeline capabilities and figure out how to compute these in a derived way. The cleanest approach is to compute regime flags as indicator columns in the analytical series using `montarPipelineIndicadores` (rolling percentile via custom Rhai script) or direct `materializarIndicador` calls. Let me see what's available and pick the right path.The Rhai sandbox doesn't have `pct_change`. Let me probe what primitives are available by testing common ones, and compute returns differently — `close[0] / close[-1] - 1` style or via arithmetic ops in the pipeline.The Rhai vectorized sandbox is intentionally limited — no shift/lag/returns primitives, just standard TA function calls like `atr(high, low, close, 14)`. I'll pivot: build the regime classification via the pipeline combinator (which supports arithmetic ops, conditional logic, and standard TA-only steps) and use the `mom` (momentum) and `roc`-like primitives the framework exposes. Let me probe what TA functions ARE valid in this sandbox and what pipeline ops work.Good — clear picture now. The Rhai vectorized sandbox only supports a small set of named TA primitives (sma, ema, rsi, macd, cmo, mfi, adx, donchian, obv, wma, hma, kama, rma). No `shift`/`diff`/`returns`/`rolling_rank`, so ret_1d and atr_percentile can't go through `materializarIndicador`. But I have plans B and C:

- **Plan B**: Build the analytical series (returns, regime flags, ATR percentile) using the **pipeline combinator** with built-in arithmetic/conditional/compose ops, which DO support cross-series `subtract`/`divide`. There's also a `step` for `rolling-quantile-cuts` (`step_44` with `cortes`) — I'll use that for ATR percentile (terciles).
- **Plan C**: Use the backtest engine's `ctMomento` (CT Momento) — likely returns/momentum-based — and `ctBacktest`'s `indicadores_receitas` mechanism to read pseudo-returns from Rhai strategy context (where indexing IS available — `close[0]`, `close[1]`).

Let me investigate `ctMomento` and explore the pipeline ops to choose the cleanest path.`ctMomento` works on intraday fine series and aggregates upward — not the right shape for daily data regime analysis. So it's not the clean path.

The cleanest path is the pipeline, which **does** support `combinar_aritmetica` (subtract/divide), conditional steps (`condicional`), rolling quantile terciles (`quantis` op with `cortes`), and standard TA roster (sma, ema, rsi, macd, adx, atr, cmo, mfi, donchian, kama, hma, wma, rma, bollinger). Let me check what RANK-related step types are available — I need to scan pipeline ops for anything like rolling rank / rolling-quantile-cuts.The TypeScript narrow types are too strict — step "compose" is stepObj1 and step "sma" is stepObj4, but TS is widening to obj66. Let me bypass the static type via `as any` casts so the schema accepts the runtime shape, OR run each step type individually. Let me first make sure I understand the shape — the schema showed obj0=arithmetic, obj1=compose, obj4=sma-column-period, obj5=atr, obj6=sma, etc. So a single step object has differentiated keys. The issue is the static union needs `as any` for the test to even compile.Ah, the `id` and `column` clash — `id` is required on every step, so adding `column` separately is fine, but TS is reading the union as rejecting some. The issue is just `column` and `id` look like duplicates? No — the real error is `id` field occurring multiple times inside ONE object literal because the original object has `id:"sm"` AND I also added `id:"sm"` at the end. Let me drop the redundant `id` in the props spread (the `id` was already in the early spot).The error message gives a key clue: `step missing 'op' discriminator`. So each step object needs an explicit `op` field that names the operation kind. Let me re-examine the schema — the step unions have a discriminator field. Looking at obj0 (arithmetic) the keys were `coluna_saida`, `id`, `operacao`, `parcelas` — no obvious discriminator. But obj1 (compose) keys were `columns`, `id`. obj4 (sma-style) keys: `column`, `id`, `period`, `source`. So the discriminator must be added externally as `op`. Let me try with `op` set to a sensible operation name like `combinar_aritmetica`, `compor`, `sma`, `atr`, `rsi`, `macd`, `quantis`, `condicional`.Progress! `op: "atr"` works. The unknown op error tells me real op names. Let me probe more op names for compose, sma, combinar_aritmetica, condicional, and quantis.Found three op discriminators so far: `compose`, `sma`, `combinar_aritmetica`. Let me find the rest — quantis (rolling tercile), condicional, and verify other TA ops (atr, rsi, macd, ema, cmo, mfi, donchian, etc.).Excellent —confirmed working ops: `sma`, `ema`, `wma`, `hma`, `kama`, `rsi`,

---

> **Disclaimer:** This content was created with the assistance of generative artificial intelligence. It is the creative material of the author and does not reflect the opinion, position, or endorsement of verida.trade or CT Lab. Not financial advice. verida.trade does not provide investment recommendations, portfolio management, or brokerage services. No predictions or guarantees of future results. Backtests and indicators reflect historical data and do not guarantee future performance. All trading and investment decisions are made solely by the user, who assumes full responsibility for all risks, including potential loss of capital. This material is intended for educational and research purposes only. Before making any financial decision, consult a licensed professional in your jurisdiction.

